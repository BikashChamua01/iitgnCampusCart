import React from "react";

const MylistingCard = ({ product }) => {
  const {
    title,
    description,
    images,
    pricce,
    originalPrice,
    category,
    condition,
  } = product;
  return <div>Hello</div>;
};

export default MylistingCard;
